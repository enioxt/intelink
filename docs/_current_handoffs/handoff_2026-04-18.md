# Handoff — 2026-04-18 (Deploy + Auth + Migration Standalone — MERGE DE SESSÕES)

> Merge de duas sessões paralelas: esta (deploy/auth/migration) + outra (chatbot SSOT).
> Próxima sessão: trabalhar no SSOT do chatbot intelink em `/home/enio/intelink/`.

---

## Accomplished

### Deploy + Auth
- **FIX-001** `8fa97c5` — `supabase-client.ts` singleton corrigido (190 GoTrueClient → 1, lógica invertida)
- **FIX-002** `8fa97c5` — spinner infinito no login corrigido (try/catch/finally em `checkSession`)
- **FIX-003** `8fa97c5`+`42c5332` — middleware: manifest.json, sw.js, icons/, api/health em PUBLIC_PATHS
- **AUTH-002-TOTP** `8fa97c5` — TOTP MFA real em `/settings/security` (WebAuthn não existe no Supabase)
- **AUTH-013d** `8fa97c5` — Telegram Login Widget injetado no login
- **Dockerfile** `e42057a` — migrado npm/Node → Bun builder + node runner com ARGs
- **VPS Deploy** — container `intelink-web` Up (healthy) em `intelink.ia.br` ✅
- **BOT-003** — `telegram_chat_id = 171767219` já em Supabase para enioxt@gmail.com ✅
- **OPENROUTER** — nova chave em `.env.local` VPS + restart → minimax/minimax-m1 responde ✅

### Chatbot SSOT (sessão paralela — outro agente)
- **`/api/_internal/discover`** `9587dca` — endpoint de descoberta de capabilities
- **MiniMax M2.5** `9587dca` — primary model em `lib/prompts/registry.ts`
- **middleware** `510f1e1` — `/api/_internal` em PUBLIC_PATHS

### Migration Standalone
- **MERGE-001** `7755d72` — 33 arquivos `lib/intelligence/` + 12 libs absorvidos do legacy
- **Standalone criado** `dcaf014` — `/home/enio/intelink/` com git próprio (saiu do egos-lab)
- **Assets absorvidos** `070c8ef` — 5 libs, 11 hooks, 3 componentes, infra, docs, scripts
- **README + MIGRATION_HISTORY** `9585d2f` — documentação completa para agentes de IA
- **Legacy arquivado** — `/home/enio/_archived/intelink-legacy-2026-04-18/` (fora do workspace)
- **egos-lab/egos-inteligencia** — marcado ARCHIVED
- **HARVEST P105..P107** `fba4e2b` — padrões de migração, arquivo, OpenRouter auth gate

---

## Estrutura final

```
/home/enio/intelink/                    ← CANONICAL ✅ (standalone, git próprio)
/home/enio/_archived/
  intelink-legacy-2026-04-18/           ← ARQUIVO 🔒 (fora do workspace)
/home/enio/egos-lab/apps/
  egos-inteligencia/  (ARCHIVED.md)     ← ARQUIVADO
  intelink-api/                         ← API Python FastAPI
VPS: /opt/intelink-nextjs/              ← produção
```

---

## Next Steps (P0 primeiro)

1. **[P0] rsync VPS** — sincronizar `/home/enio/intelink/` → `/opt/intelink-nextjs/` + rebuild para refletir migration + chatbot changes
2. **[P0] Testar `/agente` Telegram** — confirmar minimax/minimax-m1 com nova key
3. **[P0] Chatbot SSOT integration** — integrar SSOT do chatbot em `lib/intelink/` (ver ai-router.ts)
4. **[P1] AUTH-013a** — `/setdomain intelink.ia.br` no BotFather (ação manual)
5. **[P2] DHPP-SCHEMA-001** — Cypher schema Neo4j para entidades DHPP
6. **[P2] VALID-001** — 1 caso DHPP real completo end-to-end

---

## Mensagem para agente Chatbot SSOT

**LEIA ANTES DE CONTINUAR:**

O canonical do Intelink foi migrado para standalone. Novo path:

```
/home/enio/intelink/    ← TRABALHE AQUI
```

**Suas mudanças anteriores (9587dca + 510f1e1 no egos-lab) JÁ ESTÃO no canonical:**
- `app/api/_internal/discover/route.ts` ✅
- `lib/prompts/registry.ts` (MiniMax M2.5) ✅
- `middleware.ts` (PUBLIC_PATHS + /api/_internal) ✅

**Não editar mais `egos-lab/apps/egos-inteligencia/`** — tem ARCHIVED.md lá.

Próximo: integrar SSOT do chatbot em `/home/enio/intelink/lib/intelink/` ou `lib/prompts/`. O `ai-router.ts` e `agente.ts` já existem lá como ponto de entrada.

---

## Environment State

| Item | Estado |
|------|--------|
| `intelink.ia.br/login` | 200 ✅ |
| Container `intelink-web` | Up (healthy) ✅ |
| TypeScript standalone | 0 errors ✅ |
| OpenRouter key | Nova, testada ✅ |
| `telegram_chat_id` Supabase | 171767219 ✅ |
| Legacy arquivado | `_archived/intelink-legacy-2026-04-18/` ✅ |
| HARVEST | P105..P107 commitados ✅ |
