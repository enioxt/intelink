# Handoff — 2026-04-18 (Intelink Deploy + Auth Fixes)

## Accomplished

- **DASH-001** `c3dd054` — grupos criminais dashboard + ForceGraph2D (`/inteligencia/grupos`)
- **MAP-001** `c3dd054` — distribuição ocorrências por município/tipo/timeline (`/inteligencia/mapa`)
- **FIX-001** `8fa97c5` — `supabase-client.ts` singleton corrigido (190 GoTrueClient → 1, lógica invertida no guard de produção)
- **FIX-002** `8fa97c5` — spinner infinito no login corrigido (try/catch/finally em `checkSession`)
- **FIX-003** `8fa97c5` + `42c5332` — middleware: `/manifest.json`, `/sw.js`, `/icons/`, `/api/health` em `PUBLIC_PATHS`
- **AUTH-002-TOTP** `8fa97c5` — substituiu WebAuthn (não existe no Supabase) por TOTP MFA em `/settings/security`
- **AUTH-013d** `8fa97c5` — Telegram Login Widget injetado no login quando `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` configurado
- **Dockerfile** `e42057a` — migrado de npm/Node para Bun builder + node runner com ARGs para `NEXT_PUBLIC_*`
- **LEGACY** — VPS limpo: `/opt/egos-lab/apps/intelink/`, `/opt/egos-lab/apps/telegram-bot/` removidos; PM2 `egos-telegram` deletado
- **DOCS** `eec9ba5` — `docs/VPS_ARCHITECTURE.md`, `docs/BOT_ARCHITECTURE.md`, `apps/web/.env.example`
- **Deploy VPS** — container `intelink-web` Up (healthy) em `intelink.ia.br`; healthcheck usa `/login` (200)

## Blocker

**BOT-OPENROUTER** — `/agente` no Telegram retorna "OPENROUTER_API_KEY não configurada" apesar de:
- `docker exec intelink-web env | grep OPENROUTER` → key presente
- `agente.ts:123` lê `process.env.OPENROUTER_API_KEY` dentro da função
- Root cause provável: build usa código ANTIGO do VPS em `/opt/intelink-nextjs/lib/` (novo código foi rsynced para `/opt/intelink-nextjs/app/lib/` — subdir errado)
- **Fix:** `rsync -av --exclude='node_modules' .../apps/web/ root@VPS:/opt/intelink-nextjs/` (sem `app/` no destino), rebuild

**AUTH-013a** — BotFather `/setdomain intelink.ia.br` para `IntelinkBOT` — ação manual pendente

## Chat ID (BOT-003 — RESOLVIDO via Supabase)

`telegram_chat_id = 171767219` já em `intelink_unit_members` para `enioxt@gmail.com` (super_admin). BOT-003 encerrado.

## Next Steps (prioridade)

1. **BOT-OPENROUTER fix** — rsync correto + rebuild VPS (30min)
2. **AUTH-013a** — `/setdomain intelink.ia.br` no BotFather manualmente
3. **Testar /agente** após fix
4. Continuar SSOT chatbot (outra janela em andamento)

## Environment State

- `intelink.ia.br/login` → 200 ✅
- Container: `Up (healthy)` ✅
- TypeScript: 0 errors ✅
- OPENROUTER no container env: presente ✅ (mas não chegando ao código — blocker)
- `telegram_chat_id` em Supabase: 171767219 ✅
