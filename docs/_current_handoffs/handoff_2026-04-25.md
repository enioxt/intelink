# HANDOFF — Sessão 2026-04-25 (DATA-004 + MERGE + CENTRAL completa)
**Branch:** main | **HEAD:** `e4073e3` | **Sync:** ✅ origin/main | **Deploy:** intelink.ia.br healthy

---

## 🎯 ESTADO PARA MERGE COM OUTRA JANELA

Esta sessão (Janela A — sonnet 4.6) é a **fonte da verdade da camada de produção**.
Tudo aqui está deployado, testado via curl, e mudanças no Neo4j são irreversíveis.

### Verificação rápida para o agente da Janela B
```bash
cd /home/enio/intelink && git log --oneline --since='3 days ago'
# Espere ver: e4073e3, 0e60812, bf9e212, 3ec3621, 135f981, 90a16f9, ...
git rev-parse HEAD
# Espere: e4073e317c3927bf6deba33f850bbab01b46ac28
```

Se Janela B tem commits adicionais sobre `e4073e3`: faça `git log e4073e3..HEAD` lá e copie a lista para mim.

---

## ✅ ACCOMPLISHED esta sessão (4 commits)

| Commit | Categoria | Descrição |
|--------|-----------|-----------|
| `3ec3621` | feat | DATA-004 + MERGE-001..003 + CENTRAL-001/002 + LEGACY-001 |
| `bf9e212` | fix | merge endpoint: neo4j.int() para limit; SAME_AS confirmados |
| `0e60812` | fix | veículos: campos plate/make/model em vez de placa/marca/modelo |
| `e4073e3` | chore | TASKS.md atualizado: 9 tasks marcadas complete |

### Mudanças no banco Neo4j (irreversíveis sem rollback explícito)
- **213 CPFs** normalizados de `111.222.333-44` → `11122233344`
- **1.803 SAME_AS** criados com `reason: "cpf_norm", confidence: 0.95`
- **0 dados perdidos** — apenas pontuação removida onde sem conflito; SAME_AS criado onde havia conflito

### Endpoints novos em produção
```
GET  /api/neo4j/veiculos    — 665 veículos paginados, filtro por placa/modelo/marca
GET  /api/neo4j/vinculos    — 19.711 vínculos paginados, filtro por tipo
GET  /api/neo4j/merge       — candidatos SAME_AS para revisão humana
POST /api/neo4j/merge       — mescla 2 Person nodes (auth required)
```

### UI nova em /central
```
Tabs ativas: Pessoas | Ocorrências | Fotos | Veículos | Vínculos | ⚠ Merge | Operações
```

---

## 🔄 IN PROGRESS

**MERGE-004** — POST /api/neo4j/merge usa `apoc.create.relationship` para reroute de relações. APOC pode não estar instalado no container intelink-neo4j. Catch falha graciosamente (retorna 0 rerouted) mas merge fica incompleto.

**Verificar APOC no container:**
```bash
ssh -i ~/.ssh/hetzner_ed25519 root@204.168.217.125 \
  "docker exec intelink-neo4j cypher-shell -u neo4j -p 'IntelinkReds2026!' 'CALL apoc.help(\"create\")' 2>&1 | head -3"
```

---

## ⏭️ NEXT STEPS (P0 → P2)

### P0 (próxima sessão)
1. **MERGE-004** — verificar APOC, se ausente: instalar via `apoc.cypher.runMany` ou usar approach manual com 2 queries por relação
2. **DESIGN-001** — importar componentes de `~/Downloads/egos (7)/nextjs-export/`:
   - `ChatPanel` (216 LOC) — superior ao chat atual
   - `CommandPalette` (87 LOC) — pode substituir GlobalSearch quando ações > busca
   - `DrillDownSheet` — modal lateral para detalhe sem sair da página
3. **UI-003** — verificar perfis com dados incompletos: query `MATCH (p:Person) WHERE p.cpf IS NULL AND p.nome_mae IS NULL RETURN count(p)` deve guiar prioridades

### P1 (médio prazo)
4. **LEGACY-002..005** — atualizar /investigation/[id], /graph/[id], /propostas para ILHeader (visual)
5. **VALID-001** — Enio usa 1 caso DHPP real end-to-end (busca → grafo → laudo)
6. **VALID-002** — Lídia onboarding sem treinamento em 30min

### P2 (longo prazo)
7. **DHPP-ETL-001** — pipeline 115 PDFs/DOCX → BERTimbau NER → Neo4j
8. **Multi-tenant** — quando 3ª delegacia confirmada

---

## 🗂️ ARQUITETURA — Recap para merge

### Camada de dados (Neo4j em prod)
| Label | Total | SSOT field |
|-------|-------|------------|
| Person | 16.140 | `cpf` (11 dígitos) |
| Occurrence | 2.092 | `reds_number` |
| Photo | 2.718 | `filename` |
| Vehicle | 665 | `plate` |

### Lib SSOT (lib/normalize/identifiers.ts)
- `normalizeCPF` / `formatCPF` — 11 dígitos sem separadores
- `normalizePhone` — DDD com 0 + 9 dígito + 8 dígitos (BR), E.164 (US, INT)
- `formatSource` — REDS_HOMICIDIO → "REDS Homicídio" etc
- `isPlaceholderName` — detecta "A APURAR", "DESCONHECIDO" etc

### Design system (lib/design/tokens.ts)
- `IL` palette: bg0/1/2, surf, ink/ink2/dim, cyan, blue, amber, red, purple
- `CONF` levels: reds (azul), confirmed (verde), probable (amber), unconfirmed (laranja)
- Componentes: `ILCard`, `ILTag`, `ConfBadge`, `GlobalSearch` (Ctrl+K), `ILHeader`

### IDs vs UUIDs (NÃO MISTURAR)
- **Neo4j elementId** (`4:uuid:seq`) → rotas `/pessoa/[id]`, `/central`, `/graph/[id]`
- **Supabase UUID** → rotas `/investigation/[id]`, `/api/v2/auth/*`
- `decodeURIComponent(rawId)` obrigatório em pages que recebem elementId

---

## 🛡️ Pontos de risco para o merge

1. **Não toque em `lib/auth/*`** — `csrf.ts` e `api-security.ts` foram corrigidos com cuidado para reconhecer `intelink_access` JWT. Reverter quebra todo o login.

2. **Não toque em `scripts/deploy-hetzner.sh`** — path correto `/opt/intelink-nextjs`, backup automático do `.env`. Se outra janela tem versão diferente, mantenha esta.

3. **Não toque em `app/api/v2/auth/*`** — funcional, em produção, login do `enioxt@gmail.com` validado.

4. **Não rode normalize_cpf.py de novo** — já rodou, criou os 1.803 SAME_AS. Rodar de novo não duplica (idempotente) mas é desnecessário.

---

## 📍 Localização

- **Repo local:** `/home/enio/intelink`
- **GitHub:** `github.com/enioxt/intelink` branch `main`
- **VPS:** Hetzner `204.168.217.125` path `/opt/intelink-nextjs/`
- **URL pública:** `https://intelink.ia.br` (+ `www.intelink.ia.br`)
- **Neo4j prod:** porta interna `127.0.0.1:7690` (bolt) / `7475` (browser)
- **Supabase:** project `lhscgsqhiooyatkebose` (egos-lab)

---

## 📞 Como o agente da Janela B deve proceder

1. Cole os últimos 5-10 commits dele (`git log --oneline -10`) na próxima mensagem
2. Liste arquivos modificados não commitados (`git status --short`)
3. Indique qual feature ele estava trabalhando
4. Eu faço o `merge-base` e identifico exatamente o que cherry-pickar

**Não execute `git pull` ou `git merge` no lado dele antes de eu validar** — pode haver conflito em arquivos que reescrevemos aqui (auth, design system, central).

---

**Último commit:** `e4073e3` | **Sincronizado com:** `origin/main` | **Working tree:** clean
